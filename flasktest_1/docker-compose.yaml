version: '3'

services:
  flask-app:
    build: .
    container_name: flask-app
    restart: always
    environment:
      - "VIRTUAL_HOST=hantsawilliams.com,www.hantsawilliams.com"
      - "VIRTUAL_PORT=5000"
    expose:
      - "5000"
    networks:
      my-network:
        aliases:
          - flask-app
